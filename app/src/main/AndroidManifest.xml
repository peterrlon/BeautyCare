<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.beautycare">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/beautycare_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".main.FirstActivity"
            android:exported="true" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".makeup.makeupIndex"></activity>

        <activity android:name=".makeup.MakeupDetails"></activity>

        <activity android:name=".strategy.StrategyMain"></activity>

        <activity android:name=".strategy.StrategyDetails"></activity>

        <activity android:name=".mall.MallActivity"></activity>

        <activity android:name=".mall.MallDetail"></activity>

        <activity android:name=".discount.DiscountIndex"></activity>

        <activity android:name=".discount.DiscountDetails"></activity>


        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA7E_zW91dyw2AGg_nSnSAMvzF_ZEeQb1M" />

    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />


</manifest>
